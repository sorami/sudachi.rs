name: 'Run in ManyLinux aarch64 Container'
description: 'Build Wheels inside ManyLinux 2014 aarch64 container'
branding:
  color: blue
  icon: upload-cloud
inputs:
  script:
    description: 'Path to the script'
    required: true
runs:
  using: 'docker'
  image: 'manylinux_aarch64.Dockerfile'
  args:
    - ${{ inputs.script }}
