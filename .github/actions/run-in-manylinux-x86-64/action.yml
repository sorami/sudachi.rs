name: 'Run in ManyLinux x86_64 Container'
description: 'Build Wheels inside ManyLinux 2014 x86_64 container'
branding:
  color: blue
  icon: upload-cloud
inputs:
  script:
    description: 'Path to the script'
    required: true
runs:
  using: 'docker'
  image: 'manylinux_x86_64.Dockerfile'
  args:
    - ${{ inputs.script }}
